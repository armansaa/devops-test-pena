apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: node-app-raw
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - raw-app
  selector:
    matchLabels:
      app: node-app
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 15s
